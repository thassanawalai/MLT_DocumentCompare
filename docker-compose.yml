services:
  backend:
    build: ./backend
    ports:
      - "10000:10000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped